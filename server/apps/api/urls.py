from django.urls import path

from . import views

app_name = 'api'

url_patterns = [
    # path('toggle-cart/',views.CartToggleView.as_view(),name='toggle-cart')
    # path('toggle-table/',views.TableToggleView.as_view(),name='toggle-table')
]